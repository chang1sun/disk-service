package common

const RequestSuccessCode = 200

// Debug level code
const (
	InvalidDataTypeCode int32 = 4001
)

// Debug level msg
const (
	InvalidDataTypeMsg = "[debug]invalid data type, proto.Message is expected"
)
